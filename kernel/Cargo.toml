[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "kernel"
test = true
bench = false

[dependencies]
bootloader_api = "0.11"
font8x8 = { version = "0.3.1", default-features = false, features = ["unicode"] }
spinning_top = "0.3.0"

[build-dependencies]
cc = "1.2.57"