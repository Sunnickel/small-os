[toolchain]
channel = "nightly"
profile = "minimal"

components = [
    "rust-src",
    "rustfmt",
    "clippy",
    "llvm-tools-preview",
    "rust-analyzer"
]

targets = [
    "x86_64-unknown-none"
]